# Laura Félix Portfolio — Phase 1

Home-only foundation.

- Keeps the uploaded `interior-room.glb` as the main 3D environment.
- Keeps `planet.glb` and `spaceship.glb` for the existing intro/scene.
- Removes the old Projects, Technologies, About and Contact sections from the Home page.
- Removes repeated section links from the desktop navbar.
- Adds four Home navigation cards that will later open independent HTML pages.
- Cards are positioned on the right architectural area so they do not cover the panoramic window.
- Typography and card contrast are tuned for the cool white / violet lighting of the room.

The target pages (`projects.html`, `technologies.html`, `about.html`, `contact.html`) are intentionally not created yet. They will be built one by one in later phases.
